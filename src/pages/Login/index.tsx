import LoginForm from '../../components/Login/LoginForm';
import { ContainerStyle } from '../../style/global';

const LoginPage = () => {
  return (
    <ContainerStyle>
      <LoginForm />
    </ContainerStyle>
  );
};

export default LoginPage;
